# v0.7.0 邀请制用户系统交接（2026-08-12）

## 接手目标

在当前未提交工作树上完成 v0.7.0 的审查整改、验收、文档收尾、提交和 annotated tag 发布。完整产品意图以本次任务中的「v0.7.0 邀请制用户系统与三级权限」计划为准；领域语言见 [`../../CONTEXT.md`](../../CONTEXT.md)，不可逆决策见 [`../adr/0001-single-sqlite-public-private-data.md`](../adr/0001-single-sqlite-public-private-data.md) 与 [`../adr/0002-admin-account-is-credential-source.md`](../adr/0002-admin-account-is-credential-source.md)。

不要重做现有实现，也不要覆盖 `data/settings.json`。先阅读 diff 和本文件，再从“必须完成”继续。

## 仓库状态

- 工作目录：`/data/users/wenxin/arxiv`
- 分支：`dev`
- 起始稳定提交：`89ad882`
- v0.6.0 annotated tag：模块化重构基线
- 状态：**v0.7.0 已完成**——改动已提交（初始提交 `d797804`，含发布收尾修正的最终提交见本文件提交记录）、已创建 annotated tag `v0.7.0`、工作树干净。
- `source/settings/admin.py` 和 `tests/test_admin_credentials.py` 的删除是有意的：账号凭据必须以 `users` 表为唯一真源。
- `source/storage/migrations.py.orig`、`source/storage/user_migration.py.orig` 已删除；`.gitignore` 已加入 `*.orig`。
- `data/.admin-credentials.lock` 旧运行时锁已清理；`.gitignore` 使用新的 `data/.database-migration.lock`。

## 已完成实现

### 数据与迁移

- 注册连续 v4 migration，创建 `users`、`audit_events`，并为阅读清单、聊天、练习、论文导入者和 LLM 用量增加用户归属。
- 旧私有数据和手工导入论文迁移给唯一 `admin`；自动抓取论文导入者保持空值。
- 数据库约束和服务层共同保护唯一 admin，禁止删除、停用、改名、降级。
- migration 使用进程内锁和 `fcntl.flock` 跨进程锁；迁移前创建一致性 SQLite 快照。
- 旧 settings 管理密码迁入 admin 后逐字段删除，并在写 settings 前备份。
- 新增 `source/settings/strict_io.py`，供安全迁移进行严格、原子 JSON I/O。
- 已加入 bootstrap 临时密码的“数据库提交后才可领取”状态，`create_app()` 用 `finally` 获取并记录一次，目标是避免 settings 收尾失败导致密码永久丢失。

### 认证、授权和隔离

- 登录统一为用户名和密码；实现成员邀请、一次性临时密码、首次改密阻断、管理员重置、启停和永久删除。
- Flask session 保存 user id/version，30 天滑动有效；密码变化、重置、停用或删除会撤销旧 session。
- 实现统一登录失败文案、IP + 规范化用户名限流、全站 unsafe 方法 CSRF。
- 所有非静态路由显式分类为 public/member/admin；API 未登录返回 401、权限不足返回 403。
- 阅读清单、聊天、练习、答案、苏格拉底会话、进度和 LLM 用量均按当前 principal 隔离，不信任客户端 user id。
- 成员分析更新只接受 `rating`、`tags`；其他字段返回 403。
- 旧 `/api/paper/add` 已改为 admin-only，避免成员借兼容端点覆盖摘要或价值简评；成员通过 import preview/import 流程导入公开论文。
- 进度任务 id 已增加 owner 冲突保护；线程池 AI 用量使用 `copy_context()` 传播当前用户。

### 论文删除、审计、UI 和备份

- 单篇和批量论文删除保护已下沉到 storage 的单一 `BEGIN IMMEDIATE` 事务，避免 impact 检查与删除之间的 TOCTOU。
- 默认删除存在私有数据的论文返回 409；管理员 UI 已增加影响统计、确认及 `force=true` 重试；批量删除跳过受保护论文。
- 用户管理、审计日志、首次改密页面、登录用户名、角色化导航和操作入口已加入。
- 审计覆盖用户管理、导入、评分/标签、深度阅读、PDF、隐藏和删除；敏感公共正文使用 hash + 长度摘要。
- WebDAV manifest 已标明归档包含账号、私有学习数据和 settings secrets，并继续采用可信 WebDAV 假设。
- `README.md`、changelog、API、架构、运维、用户和 Agent 文档大部分已更新。

## 已完成验证（注意时间点）

在最后一轮审查整改之前，以下验收曾全部通过：

- unittest 全量；
- 随机顺序 pytest 共 256 项；
- 三次测试模块乱序；
- 路由/模板契约；
- `python scripts/check_settings_guardrail.py`，真实 `data/settings.json` 未检测到重建；
- `git diff --check`。

之后又修改了原子删除、bootstrap 密码交接、旧凭据模块、进度所有权、审计和部分 UI，尚未进行完整回归。因此不能把上述结果视为当前 HEAD 的最终通过结果。

## 必须完成

### 1. 修正现有测试与补回归测试

1. 更新 `tests/ai_test/test_web_papers_api.py` 的删除测试。它仍 mock `get_paper_private_impact()` + `delete_paper()`，应改为 mock `delete_paper_protected()` 的原子结果，并覆盖普通 409 与 `force=true` 成功。
2. 为 `source/storage/papers.py` 的 `delete_paper_protected()` 和 `batch_delete_papers_protected()` 添加真实 storage 测试：受保护论文不删除、force 级联、批量安全项删除且受保护项跳过。最好包含并发写入验证，证明 `BEGIN IMMEDIATE` 消除了检查删除竞态。
3. 在 `tests/test_storage_migrations.py` 增加真正的 multiprocessing 启动测试，而不是只用线程。多个独立进程共用同一 DB/settings，必须全部成功、仅一个 admin、仅实际创建者获得一次明文临时密码，并实际走 `fcntl.flock`。
4. 增加 post-commit failure 测试：v4 数据库提交后令 `finalize_legacy_admin_settings()` 抛错，启动应失败但 admin/schema v4 已提交，且创建进程仍可通过 `take_generated_admin_password()` 获得一次密码；第二次获取为空。另确认事务回滚时绝不输出未提交密码。
5. 扩展 `tests/ai_test/test_backup_service.py`：创建真实 v4 用户、论文及私有记录，打包并解压，验证 restored `users`、user ownership、阅读/聊天/练习记录和 settings 字节内容完整，而不是只验证 demo 表。
6. 增加全路由 guest/member/admin 参数化权限矩阵。现有测试主要验证每条路由有分类和少数样例，不足以满足三角色全路由契约。
7. 增加端点级 IDOR：成员不能通过学习 API 读取或修改另一成员的清单、聊天、quiz、answer、Socratic session 或进度。现有 storage 隔离测试不能完全替代 Web 端点测试。
8. 审计测试应验证嵌套对象被丢弃、敏感 key 递归不落库、列表受限、任意字符串截断，并验证数据库中不出现密码、聊天、答案、Prompt、API key 或完整深度阅读正文。

### 2. 完成少量代码整改

- `source/storage/users.py::record_audit_event()` 当前已过滤 secret-shaped 顶层 key 和嵌套对象，但任意字符串尚未限制长度。建议所有字符串和列表字符串截断到 500 字符，列表最多 50 项，并补测试。
- 复核 `source/storage/migrations.py` 中 `mark_generated_admin_committed()` 的调用位置：必须严格发生在 v4 transaction 成功退出之后、settings finalization 之前。
- 复核 `source/web/application.py` 的 `try/finally`：settings finalization 失败时创建者仍记录一次密码；数据库 v4 回滚时不得记录。
- 全局 `rg` 确认没有导入或调用已删除的 `source.settings.admin`、`ensure/get/set/verify/has/reset_admin_password`。
- 全局 `rg` 确认没有旧的 `get_progress(..., allow_all=...)` 调用。
- 检查 `update_progress()` 的布尔失败是否在所有可控 task id 写入方得到合理处理或至少不会覆盖其他用户记录。

### 3. 文档与仓库卫生

- `AGENTS.md` settings JSON 示例仍写 schema 3 且包含 `admin_password` 字段；改为 schema 4并删除两个旧凭据字段。
- `docs/developer-guide.md` 的 current `reading_list`、`paper_chat_messages`、`paper_quiz_sessions` DDL 仍缺 `user_id` 和 `(user_id, paper_id)` 唯一约束说明。
- `docs/architecture.md` 仍有“单用户本地部署”描述，改为邀请制单机/实验室共用模型。
- 根 `app.py` 当前声称 `gunicorn app:app` 可直接用于 WSGI，但模块级 app 不调用 `init_db()` 或调度器。不要在 import 时初始化真实运行时；建议把说明改为使用工厂（例如单进程 `gunicorn 'app:create_app()'`）并明确调度器的单进程语义。
- 更新 `AGENTS.md` 中可能过期的测试文件数量及旧管理密码 API 描述。
- 删除两个 `.orig` 文件和旧 `.admin-credentials.lock`；确认新的 migration lock 已被忽略。
- 仔细检查 `git status --untracked-files=all`，不得提交真实 DB、settings、备份、PDF 或临时密码。

## 建议执行顺序

1. 阅读 `git diff`、本交接和两份 ADR，确认不覆盖现有实现。
2. 先完成上述代码小修和文档收尾。
3. 用 TDD 补原子删除、多进程迁移、post-commit failure、备份恢复、权限矩阵、IDOR、审计测试。
4. 运行聚焦测试，修复失败后再运行完整验收。
5. 执行最终规格/规范双审查，处理所有 HIGH/MEDIUM finding。
6. 检查最终 diff，只提交产品代码、测试和文档。
7. 提交 v0.7.0 改动，然后创建 annotated tag `v0.7.0`。不要在测试全部通过前打 tag。

## 最终验收命令

```bash
python scripts/run_all_tests.py
python scripts/check_settings_guardrail.py
git diff --check
git status --short --untracked-files=all
```

此外确认：完整 unittest、随机 pytest、三次模块乱序、路由/模板契约全部由 `run_all_tests.py` 实际成功运行；若脚本输出不能证明某一项，单独执行相应测试。

## 发布完成条件

- 所有测试和 settings guardrail 通过。
- 当前 settings 未被模板重建，未泄露或提交任何秘密。
- guest/member/admin 权限矩阵、私有数据 IDOR、原子论文删除和迁移失败恢复均有测试证据。
- 文档明确 users 是唯一凭据真源、隐藏是展示属性而非访问控制、备份包含账号和私有数据。
- 工作树无意外运行时文件或 `.orig` 文件。
- v0.7.0 改动已提交，并在该完成提交上创建 annotated tag `v0.7.0`。

## Suggested skills

- `implement`：按本交接继续实现和完成验收。
- `tdd`：先补迁移、并发删除、权限和 IDOR 回归测试。
- `review`：发布前并行做 Standards 与 Spec 双审查。
- `diagnosing-bugs`：完整测试若出现跨模块或并发失败时使用。

## 安全提醒

- 不要整体重写 `data/settings.json`；任何必要的真实配置逐字段修改都必须先备份，并在完成后运行 guardrail。
- 不要输出、记录或提交临时密码、密码哈希、session secret、API key、SMTP/WebDAV 凭据或私有学习正文。
- 不要使用 `git reset --hard`、`git checkout --` 或其他会丢失当前未提交实现的命令。
